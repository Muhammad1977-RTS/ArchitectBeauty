class AppConfig {
  // Меняйте только эту строку при смене домена/сервера
  static const String domain = 'nexa.quest';

  static const String apiBaseUrl = 'https://$domain/api';
  static const String wsBaseUrl = 'https://$domain';
}
